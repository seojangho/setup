# [Created by task 2.4.4 7/28/2015 19:02:16]
# Taskwarrior program configuration file.
# For more documentation, see http://taskwarrior.org or try 'man task', 'man task-color',
# 'man task-sync' or 'man taskrc'

# Here is an example of entries that use the default, override and blank values
#   variable=foo   -- By specifying a value, this overrides the default
#   variable=      -- By specifying no value, this means no default
#   #variable=foo  -- By commenting out the line, or deleting it, this uses the default

# Use the command 'task show' to see all defaults and overrides

# Files
data.location=~/.task

# Color theme
include ~/.task/theme

# taskserver
taskd.credentials=users\/jangho\/78628884-59de-44ed-a0cf-dc20dbea98f3
taskd.ca=\/home\/jangho\/.task\/ca.cert
taskd.certificate=\/home\/jangho\/.task\/client.cert
taskd.key=\/home\/jangho\/.task\/client.key
taskd.server=mango.taskd.ca.jangho.kr:53589

# report next
report.next.description=TODO
report.next.filter=status:pending -BLOCKED -SCHEDULED
report.next.columns=id,due.remaining,description,tags,project
report.next.labels=ID,Due,Description,Tags,Project
report.next.sort=urgency-

# report schedule
report.schedule.description=SCHEDULE
report.schedule.filter=status:pending +SCHEDULED
report.schedule.columns=id,scheduled.formatted,due.remaining,description,tags,project
report.schedule.labels=ID,On,Due,Description,Tags,Project
report.schedule.sort=scheduled+
report.schedule.dateformat=M/D H:N

# report await
report.await.description=AWAIT
report.await.filter=status:waiting
report.await.columns=id,wait.remaining,due.remaining,description,tags,project
report.await.labels=ID,Start,Due,Description,Tags,Project
report.await.sort=wait+

# others
urgency.tags.coefficient=0.0
urgency.blocking.coefficient=2.0
