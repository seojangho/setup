#!/bin/bash

# usage: peach-qemu-run <NUM_CORES> <NUM_THREADS> <MEM_IN_MEGABYTES> <OTHER_PARAMS...>

set -euo pipefail

NUM_CORES=$1
NUM_THREADS=$2

__peach-qemu-run-vgpu "$@" &
sleep 60s
__peach-qemu-pinning "$((NUM_CORES * NUM_THREADS + 1))" 2 2 0
wait
exit 0
