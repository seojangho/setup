function open() {
  if [[ -d "$@" ]]; then
    gtk-launch org.gnome.Nautilus "$@"
  else
    xdg-open "$@"
  fi
}
op() {
  open $@
  exit $?
}
# usage: echo 'something' | clip
alias clip="perl -p -e 'chomp if eof' | xclip -selection clipbaord"
