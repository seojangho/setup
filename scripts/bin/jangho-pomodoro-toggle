#!/bin/bash

if killall jangho-pomodoro
then
  kdialog --icon ktimer --title "Stopped pomodoro" --passivepopup "Come back when you're ready" 5
  exit 0
fi

exec "$HOME"/.setup/scripts/bin/jangho-pomodoro
