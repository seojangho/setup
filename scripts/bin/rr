#!/usr/bin/perl

use warnings;
use strict;

my $brightness = ($ARGV[0] // 100) * 0.95 * 0.01;
open (my $fh, '>', "$ENV{HOME}/.lime-xrandr-control") or die "open: $!";
print $fh $brightness;
close $fh;
system("xrandr --output HDMI-2 --gamma 1:1:1 --brightness $brightness");

exit 0
