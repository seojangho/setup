#!/bin/bash

# usage: unreadd [INTERVAL]

interval=${1:-1m}

while :; do
  u=$(unread)
  if [[ "$u" -ne 0 ]]; then
    jdunstify unread "Unread mail: ${u}"
  else
    jdunstify-clear unread
  fi
  sleep "${interval}"
done
